apiVersion: apps/v1
kind: Deployment
metadata:
  name: tunnel-client-mongodb
  namespace: tunnel-system
  labels:
    app.kubernetes.io/name: tunnel-client-mongodb
    app.kubernetes.io/component: tunnel-client
    app.kubernetes.io/part-of: air-gapped-tunnel
    tunnel.type: mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: tunnel-client-mongodb
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tunnel-client-mongodb
        app.kubernetes.io/component: tunnel-client
        tunnel.type: mongodb
    spec:
      restartPolicy: Always
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
      - name: tunnel-client
        image: s4l10u/air-client
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: TUNNEL_TOKEN
          valueFrom:
            secretKeyRef:
              name: tunnel-auth
              key: TUNNEL_TOKEN
        - name: TUNNEL_SERVER_URL
          valueFrom:
            configMapKeyRef:
              name: tunnel-config
              key: TUNNEL_SERVER_URL
        - name: CLIENT_ID
          valueFrom:
            configMapKeyRef:
              name: tunnel-config
              key: CLIENT_ID_MONGODB
        - name: SKIP_VERIFY
          valueFrom:
            configMapKeyRef:
              name: tunnel-config
              key: SKIP_VERIFY
        - name: FORWARD_MONGODB
          valueFrom:
            configMapKeyRef:
              name: tunnel-config
              key: FORWARD_MONGODB
        args:
        - "-server=$(TUNNEL_SERVER_URL)"
        - "-token=$(TUNNEL_TOKEN)"
        - "-id=$(CLIENT_ID)"
        - "-forward=$(FORWARD_MONGODB)"
        - "-skip-verify=$(SKIP_VERIFY)"
        - "-improved=true"
        - "-metrics=true"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "pgrep tunnel-client"
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "pgrep tunnel-client"
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5